apiVersion: v1
kind: Service
metadata:
  name: {{ include "FileRise.fullname" . }}
  labels:
    {{- include "FileRise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "FileRise.selectorLabels" . | nindent 4 }}
